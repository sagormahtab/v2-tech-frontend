import React from 'react';

const SubSectionTitle = ({title}) => {
    return (
        <p className="muted-clr text-center gray-bg sub-sec-border">{title}</p>
    );
};

export default SubSectionTitle;